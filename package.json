{
  "name": "erc20-faucet-dapp",
  "version": "1.0.0",
  "description": "Full-Stack ERC-20 Token Faucet DApp with Rate Limiting",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat"
  },
  "keywords": ["ethereum", "erc20", "faucet", "dapp", "web3"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3"
  }
}
